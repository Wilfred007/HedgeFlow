// Tool: read USDC vault balances per chain (PRD FR-2, FR-5).
// TODO: wrap a viem public client per ChainConfig and expose a Mastra tool
// that returns { chain, balance } for the workflow's checkBalances step.
