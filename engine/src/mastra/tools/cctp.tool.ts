// Tool: CCTP V2 burn-and-mint transfer between vaults (PRD FR-8).
// TODO: submit burn on source chain, poll Circle's attestation service, then
// submit mint on destination chain. This is the only path that moves real
// funds — keep it out of reach of the advisory agent (FR-14).
