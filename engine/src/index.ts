// Entrypoint. TODO: load env, start the poll loop (FR-5) on POLL_INTERVAL_MS
// that triggers the rebalance workflow.
import "dotenv/config";
import { logger } from "./lib/logger.js";

logger.info("HedgeFlow engine scaffold — not yet implemented");
